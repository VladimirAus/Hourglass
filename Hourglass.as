package  {		import flash.display.Sprite;	//import flash.events.Event;		public class Hourglass extends Sprite {				private var _speed:Number = 10;		private var _direction:Number = 1;				public function Hourglass() {			// constructor code			//addEventListener(Event.ENTER_FRAME, update);		}				public function set speed(s:Number):void {			_speed = s;		}				public function set dir(d:Number):void {			_direction = d;		}				public function update():void {						/*			y -= _speed;			if(y < -(height / 2))			{				y = stage.stageHeight + (height / 2) + (height * Math.random());				x = -(width / 2) + ((stage.stageWidth + width) * Math.random());			}			*/									//if ((rotation != 0) && (rotation != 180)) {				rotation += _speed*_direction;			//}		}	}	}